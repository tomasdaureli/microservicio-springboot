server:
    port: 8081
spring:
    cloud:
        config:
            server:
                git:
                    default-label: config-server
                    uri: https://github.com/tomasdaureli/microservicios-springboot
                    search-paths: config-data
    application:
        name: config-service
